


// Instantiate the module
memoryStage instance_name (
    .clk(clk), 
    .zero(zero), 
    .branch(branch), 
    .MemRead(MemRead), 
    .MemWrite(MemWrite), 
    .aluRslt(aluRslt), 
    .datafrmreg(datafrmreg), 
    .data_mem(data_mem)
    );


